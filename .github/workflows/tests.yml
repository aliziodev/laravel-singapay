name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.2', '8.3', '8.4']
        laravel: ['11.*', '12.*', '13.*']
        exclude:
          - php: '8.2'
            laravel: '13.*'   # Laravel 13 requires PHP 8.3+

    name: PHP ${{ matrix.php }} — Laravel ${{ matrix.laravel }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, pdo, sqlite3
          coverage: xdebug
          tools: composer:v2

      - name: Cache Composer
        uses: actions/cache@v4
        with:
          path: ~/.composer/cache
          key: composer-php${{ matrix.php }}-laravel${{ matrix.laravel }}-${{ hashFiles('composer.json') }}
          restore-keys: composer-php${{ matrix.php }}-laravel${{ matrix.laravel }}-

      # Laravel 11 is past its security-fix window, so its releases carry
      # unpatched advisories and newer Composer refuses to install them by
      # default. We still test against it for compatibility — the advisories
      # live in the consumer's framework, not in this package — so the audit
      # block is lifted for these cells only.
      - name: Allow post-EOL Laravel 11 in this cell
        if: matrix.laravel == '11.*'
        run: |
          composer config policy.advisories.block false \
            || php -r '$j=json_decode(file_get_contents("composer.json"),true);$j["config"]["policy"]["advisories"]["block"]=false;file_put_contents("composer.json",json_encode($j,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES));'

      - name: Install dependencies
        run: |
          composer require "laravel/framework:${{ matrix.laravel }}" --no-update --no-interaction
          composer update --prefer-dist --no-interaction --no-progress

      - name: Check code style (Pint)
        if: matrix.php == '8.4' && matrix.laravel == '13.*'
        run: vendor/bin/pint --test

      - name: Static analysis (Larastan level 8)
        if: matrix.php == '8.4' && matrix.laravel == '13.*'
        run: vendor/bin/phpstan analyse --no-progress

      - name: Run tests with coverage gate
        if: matrix.php == '8.4' && matrix.laravel == '13.*'
        run: vendor/bin/pest --compact --coverage --min=80

      - name: Run tests
        if: matrix.php != '8.4' || matrix.laravel != '13.*'
        run: vendor/bin/pest --compact
